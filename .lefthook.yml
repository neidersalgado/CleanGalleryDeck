pre-push:
  parallel: true
  commands:
    lint:
      run: ./gradlew lint
      skip:
        - merge
        - rebase
    test:
      run: ./gradlew test
      skip:
        - merge
        - rebase
